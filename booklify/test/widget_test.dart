import 'package:flutter_test/flutter_test.dart';

void main() {
  // Widget tests will be added here as features are developed.
  testWidgets('placeholder test', (WidgetTester tester) async {
    expect(1 + 1, equals(2));
  });
}
